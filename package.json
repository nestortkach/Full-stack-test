{
  "name": "full-stack-test",
  "version": "1.0.0",
  "scripts": {
    "back:start": "cd test-backend && npm install && npm run docker:up && npm run prisma:setup && npm run dev",
    "front:start": "cd test-frontend && npm install && npm run dev"
  },
  "author": "",
  "license": "ISC"
}
